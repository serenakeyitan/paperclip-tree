Stop the gardener-respond loop.

This is a no-op reminder — `/loop` is stopped by pressing `Ctrl+C` or
closing the terminal. This command exists for symmetry with
`/gardener-respond-start`.

If running under `/schedule`, remove the schedule entry.
